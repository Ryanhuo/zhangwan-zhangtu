# 技能管理

## 页面目标

- 展示项目自带技能与用户导入技能的列表与详情。
- 支持上传 `.md`、`.zip`、`.skill` 格式的技能文件，完成本地导入。

## 页面结构

- 左侧：技能目录（项目自带 / 用户导入两个分组）。
- 右侧：技能 Markdown 详情，含 frontmatter 标签展示。

## 当前边界

- 技能数据通过 `/api/skills` 接口从本地文件系统读取，不连接远程仓库。
- 导出功能通过 `/api/skills/:id/export` 下载 zip 包。
