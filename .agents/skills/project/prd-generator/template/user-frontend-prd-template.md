# [页面名称] PRD (产品需求文档)

## 1. 引言

### 1.1 背景

> 简要说明该元素/页面的核心定位、解决的问题、用户价值（控制在50字左右）。

```
[需求背景内容]
```

### 1.2 用户角色

> 简要说明不同类型的用户角色，包括但不限于：游客、个人用户、供方普通企业用户、供方会员企业用户、采方企业用户。

| 用户角色 | 描述 | 使用场景 |
|---------|------|---------|
| `[角色1]` | `[角色描述]` | `[使用场景]` |
| `[角色2]` | `[角色描述]` | `[使用场景]` |

### 1.3 功能清单

> 以表格形式梳理出所有模块、功能点及功能描述。

| 模块             | 功能点         | 功能描述       |
| ---------------- | -------------- | -------------- |
| `[功能模块标题]` | `[功能点标题]` | `[功能点描述]` |


## 2. [功能名称]需求说明

> 按功能模块循环撰写

### 2.1 [子模块名称]

#### 2.1.1 功能描述

> 详细描述该子模块的功能

#### 2.1.2 交互流程

> 描述用户操作流程，包括触发条件、操作步骤、反馈等

1. **触发条件**：[用户操作，如点击按钮、进入页面等]
2. **操作步骤**：
   - 步骤1：[描述]
   - 步骤2：[描述]
3. **操作反馈**：
   - 成功：[成功反馈，如弹窗提示、页面跳转等]
   - 失败：[失败反馈，如错误提示等]

### 2.2 [子模块名称]

> 按同样格式描述其他子模块



### 模板使用规则（AI生成时需遵守）

1. **编写说明段落**：所有 `>` 开头的段落是模块编写说明，用于指导如何填写该模块，**不要出现在最终PRD文档中**。
2. **占位符替换**：所有 `【】` 包裹的内容为占位符，需替换为实际业务内容，无内容的层级直接删除，不保留空行/空层级。
3. **格式一致性**：必须严格按照模板格式和模块填写，不要私自增加或删除模块，不能有任何偏差。
4. **内容完整性**：
   - 功能模块标题、功能点标题、功能点描述等内容需根据实际需求进行填写
   - 交互流程中的触发条件、操作步骤、操作反馈等内容需详细描述，不能有任何缺失或错误